# Spring data source properties
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://localhost:3306/db_example?serverTimezone=UTC
spring.datasource.username=your_user
spring.datasource.password=yor_password

# App Properties
app.jwtSecret=your_secret
app.jwtExpirationInMs=8640000

# Front End URL (Allowed Origin)
allowed.origin=http://localhost
